---
trigger: always_on
---

# Compatibility adapter

The canonical instructions are [`../../AGENTS.md`](../../AGENTS.md). Follow
them and their change-specific reading map before working.

Detailed, durable guidance lives in `../../SECURITY.md`, `../../docs/ai/`,
`../../docs/domain-invariants.md`, `../../docs/production-runbook.md`, and
`../../docs/decisions/`. Do not copy or redefine those rules in `.agent`.
