/**
 * Compatibility export for the former public button name.
 * AppButton is the single implementation and supports all legacy intents.
 */
export { AppButton as Button } from "./AppButton";
export type { AppButtonProps as ButtonProps } from "./AppButton";
